rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth.uid == uid;
    }
    match /call_logs/{id} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    match /spam_numbers/{phone} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
